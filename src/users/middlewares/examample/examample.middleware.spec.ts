import { ExamampleMiddleware } from './examample.middleware';

describe('ExamampleMiddleware', () => {
  it('should be defined', () => {
    expect(new ExamampleMiddleware()).toBeDefined();
  });
});
